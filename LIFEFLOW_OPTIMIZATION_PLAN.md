# LIFEFLOW 产品优化整合方案

> 生成日期：2026-07-30
> 性质：全面体检审计 + 信息架构重设计 + 优化路线图（整合文档）
> 基线：基于当前代码库实际状态（路由 / 数据库 / 导航 / 组件）与 `LIFEFLOW_GUIDE.md` 现有约定

---

## 〇、产品定位与目标用户

### 定位结论

LIFEFLOW 不是「待办清单」，而是 **目标驱动的个人管理系统（Life OS）**：

- 核心引擎是「目标五层拆解」（愿景 → 关键结果 → 策略 → 周任务 → 日行动），一切围绕目标展开
- 日程时间轴承担「执行层」（作息/课程/事项/饮水/专注/记账全落进时间轴）
- 长期主义承担「习惯与长期维度」（健康/财务/学习/心愿）
- 复盘引擎（UnifiedReviewer）承担「闭环反哺」（日/周/月/年复盘）

### 目标用户画像

| 维度 | 定义 |
|------|------|
| 主画像 | 有明确长期目标的自我提升者：备考学生（四六级/国考/考研）、职场新人（健身/理财/技能成长） |
| 核心需求 | 不是"记录生活"，而是"实现目标"——把人生目标拆到每天可执行，持续跟踪与复盘 |
| 价值主张 | 一站式「目标 → 计划 → 执行 → 复盘」个人管理闭环 |
| 使用节奏 | 早晨看首页今日执行 → 目标页拆解规划 → 日程页执行打卡 → 长期主义记录习惯 → 复盘洞察反哺 |

### 主线用户路径（作为信息架构设计的决策依据）

```
定目标(五层拆解) → 排计划(策略/周任务/日行动) → 每日执行(日程时间轴) → 记录习惯(长期主义) → 复盘洞察(首页/长期主义)
```

---

## 一、功能模块清单（覆盖全部网站）

### 1.1 核心 4-tab

| Tab | 路由 | 实际内容 | 状态 |
|-----|------|---------|------|
| 首页 | `/` | 复盘洞察 + 饮水提醒 + 今日待办 + 倒数日 + FAB | ✅ 正常 |
| 目标 | `/efficiency` | **旧版目标系统 v1**（`LifeFlowEfficiency` 库） | ⚠️ 已废弃，应切换 |
| 日程 | `/efficiency/schedule` | 时间轴（折叠显示/冲突检测/作息课程自动生成） | ✅ 正常 |
| 长期主义 | `/longtermism` | 复盘时间轴 + 9 张模块卡片 + 历史复盘 | ✅ 正常 |

### 1.2 新版目标系统（独立于 4-tab）

| 路由 | 内容 | 状态 |
|------|------|------|
| `/efficiency-v2` | 目标列表（五层拆解，AI 导入） | ✅ 新版核心 |
| `/efficiency-v2/new` | 5 步创建向导（模板 + AI 提示词导入） | ✅ 正常 |
| `/efficiency-v2/goals/[id]` | 目标详情 | ✅ 正常 |

### 1.3 长期主义模块卡片（9 张）

饮水 `/more/water`、睡眠 `/more/sleep`、记账 `/more/accounting`、训练 `/more/fitness`、饮食 `/more/diet`、养生 `/more/wellness`、体态拉伸 `/more/posture`、心愿 `/more/wishes`、记忆 `/more/ebbinghaus`

### 1.4 /more 子模块（非卡片入口，入口在 /more/projects）

| 路由 | 功能 | 备注 |
|------|------|------|
| `/more/projects` | 全部功能聚合导航 | 首页「功能模块」按钮直达 |
| `/more/calendar` | 日历月视图 | |
| `/more/countdown` | 倒数日 | 首页有卡片 |
| `/more/focus` | 专注计时（番茄钟） | 新 |
| `/more/habits` | 习惯打卡 | 新 |
| `/more/medication` | 吃药提醒 | |
| `/more/notes` | 备忘录 | |
| `/more/reminder-settings` | 默认提醒设置 | |
| `/more/review` + 6 子页 | 各模块复盘（部分为占位文案） | ⚠️ 部分占位 |
| `/more/schedule/courses` | 课程表 | |
| `/more/schedule/routines` | 作息模板组 | |
| `/more/accounting/{ledgers,assets,categories}` | 记账子管理 | |

### 1.5 其他路由

| 路由 | 功能 | 状态 |
|------|------|------|
| `/tasks` | 任务分类（日程页"分类"入口） | 正常 |
| `/reminders` | 提醒列表 | 正常 |
| `/assistant` | AI 助手（全局悬浮球进入） | 正常 |
| `/settings` | 设置（数据迁移/个性化） | 正常 |
| `/focus` | 专注（日程页快捷操作跳转） | 与 `/more/focus` 并存 |
| `/exercise` | 训练（MusclePage） | 与 `/more/fitness` 并存 |
| `/plans/[planId]` | 训练计划详情 | 归属模糊 |
| `/trash`、`/pending`、`/offline`、`/widget` | 辅助页 | 边缘功能 |
| `/plugins/*`（5 个） | 旧版插件中心 | ⚠️ 全部有新版替代 |

### 1.6 数据库全景（7 个 Dexie 实例）

| 数据库 | 表数 | 最高版本 | 状态 |
|--------|------|---------|------|
| LifeFlowHealth | 12 | v12 | ✅ 正常（T13 后旧表已清零） |
| LifeFlowDaylog | 4 | v6 | ✅ 核心（items 为主表） |
| LifeFlowGoalV2 | 5 | v2 | ✅ 新版目标核心 |
| LifeFlowEfficiency | 4 | v19 | ✅ 正常（T13 后 tasks/habits 已删，goals/scheduleTasks/projects/phases 活跃） |
| LifeFlowEbbinghaus | 3 | v1 | ✅ 新 |
| LifeFlowAccounting | 4 | v4 | ✅ 正常 |
| LifeFlowLife | 8 | v7 | ✅ 正常 |

---

## 二、现状审计报告

### 2.1 问题分级清单

| 级别 | # | 问题 | 影响 | 证据 |
|------|---|------|------|------|
| 🔴 高 | 1 | **底导「目标」tab 仍指向旧版 `/efficiency`，而新版目标在 `/efficiency-v2`** | 用户从导航进入的是废弃系统，功能双轨、数据分裂 | BottomTabBar 目标 tab = `/efficiency`；首页空态"目标"按钮 = `/efficiency-v2` |
| 🔴 高 | 2 | **旧版 `/plugins` 体系与新功能重复（5 个插件均有新版替代）** | 入口混乱、维护双份、样式割裂（旧灰白 vs 新版令牌） | plugins/habit→more/habits、plugins/finance→more/accounting 等 |
| 🔴 高 | 3 | **`LifeFlowEfficiency` 库（v15）仍在线上** | 冗余存储、迁移负担；⚠️ 审计时"6 张表无数据产出"的结论已过时——AI 助手/作息同步/任务页仍活跃写入 goals/scheduleTasks/projects（T8 已软冻结 habits/tasks 死表，完整冻结依赖 T9 迁移后） | GUIDE 明确标注"已废弃，不迁移，仅保留代码" |
| 🟠 中 | 4 | **health.db 17 张表新旧重复**：`waterRecords`/`waterLogs`、`sleepRecords`/`sleepLogs`、`fitnessRecords`+`exercises`+`muscleGroups` vs `muscleGroupsV2`+`exercisesV2`+`workoutSessions` | 数据口径分裂、代码查询易错 | 同域数据多表并存 |
| 🟠 中 | 5 | **饮水数据双轨**：`daylog.items(sourceType=water)` 与 `health.waterLogs` 并存 | 统计口径不统一（GUIDE 已多次为口径打补丁） | 首页/长期主义/饮水页三种查询 |
| 🟠 中 | 6 | **`/more` 无总入口页**，靠 `/more/projects` 动态聚合，功能可达性差 | 新功能入口深，用户找不到 | /more 目录无 page.tsx |
| 🟠 中 | 7 | **`/more/review/*` 部分子页为占位文案** | 无效页面，用户点击无收益 | review/{finance,fitness,...} 部分为"引导用 AI 助手" |
| 🟢 低 | 8 | `/focus` 与 `/more/focus`、`/exercise` 与 `/more/fitness`、`/plans/[planId]` 归属模糊 | 双入口、职责不清 | 同功能两套路由 |
| 🟢 低 | 9 | 长期主义卡片 9 张未覆盖专注/习惯/倒数日/备忘录/吃药/日历/课程/作息 | 核心习惯入口依赖 /more/projects | MODULES 数组仅 9 项 |
| 🟢 低 | 10 | `Common/TabBar.tsx`、`ui/woven/` 等组件疑似无人使用 | 死代码膨胀 | 子代理核验 |

### 2.2 优势盘点（保留）

- 目标五层拆解 + AI 导入闭环成熟（模板/提示词/解析器/冲突检测）
- 日程时间轴体验完整（作息课程自动生成/折叠/冲突/校准/复盘）
- 复盘引擎（UnifiedReviewer）已统一，首页与长期主义共用
- 数据层分离清晰（业务库拆分），版本升级流程规范（GUIDE 约束）
- 全局组件体系完善（BottomSheet/ActionSheet/Toast/Skeleton/ErrorBoundary）

---

## 三、新信息架构方案

### 3.1 架构原则

1. **目标主线**：一切入口围绕「目标 → 执行 → 长期」三层展开
2. **单一入口**：同一功能只保留一个入口、一套数据、一条路由
3. **旧版退役**：旧版功能（plugins/efficiency v1）逐步隐藏入口，路由保留但不可达
4. **渐进迁移**：不删除功能、不丢数据，先切入口后迁数据

### 3.2 新导航结构（建议）

```
底部 4-tab（保持 4 个，重定义第 2 个）
├── 首页        `/`                       （不变）
├── 目标        `/efficiency-v2`          （从 /efficiency 切换 ✅ 高优先）
├── 日程        `/efficiency/schedule`    （不变）
└── 长期主义    `/longtermism`            （不变）

功能目录（"全部功能"单一入口）
└── `/more` 总入口页（新增 page.tsx，替代 /more/projects 的隐式入口）
    ├── 习惯组：饮水/睡眠/训练/饮食/养生/体态/习惯/吃药/专注
    ├── 生活组：记账/心愿/倒数日/备忘录/日历
    ├── 学习组：记忆（艾宾浩斯）
    └── 管理组：课程表/作息/提醒设置/复盘
```

### 3.3 逐项变更与科学依据

| # | 变更 | 依据 | 风险 | 是否打破现有约定 |
|---|------|------|------|----------------|
| C1 | 底导「目标」tab 路由改为 `/efficiency-v2` | 新版是唯一活跃目标系统；首页/复盘/创建向导均指向 v2 | 低（改一行路由） | 打破「目标 tab=/efficiency」隐含约定 |
| C2 | 新增 `/more` 总入口页（静态分类目录） | /more 无 page.tsx，功能可达性差 | 低 | 否 |
| C3 | `/more/projects` 保留但降级为内部页（或合并进 /more） | 避免双"全部功能"入口 | 中 | 否 |
| C4 | 隐藏 `/plugins` 入口（路由保留，底导/首页不再链接） | 5 插件均有新版替代，避免用户进入旧版 | 低 | 打破「plugins 中心」约定（GUIDE 未明文，可执行） |
| C5 | 长期主义卡片扩展（补齐专注/习惯/吃药/倒数日） | 核心习惯入口过深 | 中 | 否 |
| C6 | 明确 `/focus` 与 `/more/focus`、`/exercise` 与 `/more/fitness` 归属（保留主入口，另一跳转） | 双入口职责不清 | 低 | 否 |
| C7 | `/more/review/*` 占位子页补充内容或从目录移除 | 无效页面 | 低 | 否 |

> 关于打破 4-tab 数量：**建议保持 4-tab**（科学依据：移动端认知负担、现有 UI 原则约定；新增 tab 收益低，功能入口应由 `/more` 目录承载）。

---

## 四、优化路线图

### 4.1 优先级模型

```
优先级分 = 用户价值 × 0.5 + 实现成本（低=高分）× 0.3 + 依赖度（少依赖=高分）× 0.2
```

### 4.2 分阶段计划

> 进度标记：✅ 已完成 · 🔄 进行中 · ⬜ 待执行

#### P0 快赢（低风险高价值，1 周内）

| # | 任务 | 改动范围 | 影响数据 | 验收标准 | 状态 |
|---|------|---------|---------|---------|------|
| T1 | 底导「目标」tab → `/efficiency-v2` | BottomTabBar.tsx 一行 | 无 | 点目标 tab 进入新版目标列表 | ✅ 已完成 |
| T2 | 首页「功能模块」按钮直达 `/more` 总入口 | 新增 /more/page.tsx + 首页链接 | 无 | /more 展示全部分类目录 | ✅ 已完成 |
| T3 | 移除 `/plugins` 对外入口（底导/首页/更多链接） | 导航与聚合页 | 无 | 站点内无 plugins 链接可达 | ✅ 已完成 |
| T4 | `/more/review/*` 占位子页移除或补 1 行真实摘要 | review 目录 | 无 | 占位页不再出现在目录 | ✅ 已完成 |

#### P1 架构整合（2-4 周）

| # | 任务 | 改动范围 | 影响数据 | 验收标准 |
|---|------|---------|---------|---------|
| T5 | 长期主义卡片扩展（+专注/习惯/吃药/倒数日） | longtermism/page.tsx | 无 | 卡片补齐，均带实时数据 | ✅ 已完成 |
| T6 | 统一饮水数据口径：明确 waterLogs 为唯一流水源，daylog water items 仅做"待办"展示 | health.db/daylog.db 查询层 | 数据不迁移，仅统一读取 | 首页/长期主义/饮水页数值一致 | ✅ 已完成 |
| T7 | health.db 旧表归档（waterRecords/sleepRecords/fitnessRecords/exercises/muscleGroups 标记 deprecated，只读） | health.db.ts | 旧数据保留只读 | 新代码不再写旧表 | ✅ 已完成 |
| T8 | efficiency.db v15 冻结（软冻结：habits 全只读 + tasks 禁增改，保留 delete 清理） | efficiency.db.ts | 旧数据保留 | 死表不再写入；goals/scheduleTasks/projects/phases 保留（AI 助手/作息同步仍依赖） | ✅ 已完成（软冻结） |

#### P2 功能增强（1-2 月）

| # | 任务 | 改动范围 | 影响数据 | 验收标准 |
|---|------|---------|---------|---------|
| T9 | efficiency v1 目标数据**一键迁移**到 GoalV2 | 迁移工具 + 设置页入口 | 迁移后旧数据保留备份 | 迁移完成率 100%，进度/周任务/日行动对齐 | ✅ 已完成 |
| T10 | 新用户引导（首次进入高亮主线路径） | 首页/目标页 | 无 | 新用户 3 步内理解主线 | ✅ 已完成 |
| T11 | `/more` 目录支持模块搜索 | /more/page.tsx | 无 | 输入关键词可定位模块 | ✅ 已完成 |
| T12 | 旧版 plugins 页面正式下线（404 重定向到新版） | 路由层 | 无 | plugins 路由全部重定向 | ✅ 已完成 |

#### P3 远期（3 月+）

| # | 任务 | 改动范围 | 影响数据 | 验收标准 |
|---|------|---------|---------|---------|
| T13 | 数据全量归一：物理删除 deprecated 死表（T7/T8 冻结表，删除前逐张核实记录数均为 0，无数据风险） | efficiency.db v19 + health.db v12（`表名: null` 显式删表） | 无（7 张表记录数核实为 0，未触发导出备份） | ✅ deprecated 表全部物理删除：效率库 6→4 表（-33.3%）、健康库 17→12 表（-29.4%），应用运行无报错 | ✅ 已完成 |
| T14 | 目标系统深度集成：GoalV2 与日程/长期主义数据打通 | goal-v2-engine | 增量 | 目标日行动 → 日程 item → 复盘链闭环 | ✅ 已完成 |
| T15 | 系统科学化重构（三件套）：a) 课堂节奏 45+5 作息 + 饮水三时段目标制（删除每小时提醒）；b) 训练/体态拉伸/功法合并为完整训练体系；c) 全系统审计 + 页面/入口重规划 | daylog.db / health.db / reviewer / 各页面 | 增量（旧 water 待办定时清理） | ①45+5 自动切分、休息防久坐；②饮水按上午/下午/晚上(睡前2h截止)三时段目标；③训练体系单入口 | ✅ 已完成（a：45+5 课堂节奏 + 饮水三时段目标制；b：训练中心三 Tab 单入口 + 旧路由 redirect；c：第一性原理审计，清理 19 个死页/占位/死代码，proxy 断链规则归零，快捷专注 query 预填） |

#### P3 续 · T16 系统单一化收尾（v2.7+，规划中）

> 背景：T15c 第一性原理审计发现 v1 目标系统（/efficiency 系列 + efficiencyDB 4 表）仍被 /tasks、AI 助手、/more/projects、首页颜色继承活跃引用，与 v2 双轨并存。用户决策：/tasks 下线入口改跳 v2；AI 动作迁移到 v2/新体系；v1 efficiencyDB 4 表数据**全部直接删除**（用户确认，无需归档）。
> 记录数已核实（2026-08-05）：efficiencyDB goals=6、projects=21、scheduleTasks=0、phases=0；GoalV2 全表 0。

| # | 任务 | 改动范围 | 影响数据 | 验收标准 | 状态 |
|---|------|---------|---------|---------|------|
| T16-1 | 日程页「分类」入口改跳 `/efficiency-v2`，/tasks 页下线（proxy 重定向 /tasks→/efficiency-v2） | efficiency/schedule/page.tsx + 删 tasks/page.tsx + proxy.ts | 无 | 分类按钮进 v2 目标页，/tasks 可达即重定向 | ✅ 已完成 |
| T16-2 | /more/projects 双目录归一：water/reminders/reminder-settings 3 处链接改指向 /more；projects 页下线（proxy /more/projects→/more）；首页 v1 projectColorMap 颜色继承依赖移除 | 3 页面 + 删 projects/page.tsx + proxy.ts + 首页 | 无 | 站点内无 /more/projects 链接，首页不读 v1 projects | ✅ 已完成 |
| T16-3 | AI 助手 v1 动作迁移：习惯打卡（v1 goals goalType=habit）→ life.db Habit（/more/habits 同源）；创建项目 → 创建 GoalV2；goals 状态查询/增删改 → GoalV2；scheduleTasks → daylog items；navigate_review → /more/review（复盘模块现位于 /more/review，非 /efficiency-v2） | AgentProvider.tsx（10+ 处 v1 引用） | 增量 | AI 动作全部落 v2/新体系，无 v1 写入 | ✅ 已完成 |
| T16-4 | /efficiency 系列页面下线：/efficiency、/efficiency/create、/efficiency/goals/[goalId]（+tasks 子页）、/efficiency/review（+Review 组件、efficiencyStore、v1-goal-migration、V1GoalMigrationDialog、components/efficiency 孤儿组件），proxy 精确重定向到 /efficiency-v2（排除保留的 /efficiency/schedule） | 删 15 文件 + proxy.ts + settings 页 + BottomTabBar | 无 | 站点内无 v1 目标页面可达；/efficiency/schedule 日程模块正常访问 | ✅ 已完成 |
| T16-5 | v1 efficiencyDB 数据处置（**执行调研修正**）：4 表并非全删 —— `projects`/`phases` 物理删除（`表名: null` + version 19→20），21 项目数据随之删除；`goals`/`scheduleTasks` 表**保留**（training-plan-generator title 匹配、routineSync/日历/日程活跃依赖）但 upgrade 回调清空 v1 历史数据（goals 6 条删后自动重建）；efficiency.db.ts 删 Project/Phase 类型与 CRUD，initializeEfficiencyDB 移除 projects 播种；ClientProviders 移除 v1 旧任务清理逻辑 | efficiency.db.ts v20 + ClientProviders + 全局引用 | 6 目标清空（可重建）；21 项目已确认删除；scheduleTasks/phases 为 0 | projects/phases 表物理消失，无代码引用；goals/scheduleTasks 正常供训练体系/作息同步使用 | ✅ 已完成 |
| T16-6 | 验证（typecheck + build + 浏览器走查：重定向/删库后无报错/AI 动作）+ GUIDE/PLAN 文档登记 + git 提交推送 | 全量 | — | 无回归，文档同步 | ✅ 已完成 |
| T16-7 | （阶段 2）功能科学化深化选点（首页核心闭环 / 复盘系统 / 具体模块），阶段 1 完成后与用户确认 | 待选点 | 待定 | 待定 | ✅ 已完成（用户选定） |
| T17-1 | 目标复盘数据层增强（`reviewer.ts` `getGoalsData`）：从「整体聚合值」升级为**目标级推进数据**——按 goal 聚合周期内日行动完成率/完成数、KR 当前达成情况（currentValue≥targetValue 计数）、active 目标 list；保留现有整体聚合以兼容现有 finding。注：GoalV2 progress 由引擎实时计算（goal-v2-engine.ts recalculateGoalProgress），**无历史快照**，故不做周期内 progress 差值 | `src/lib/brains/reviewer.ts` | 无（只读） | 返回结构含 per-goal 明细，现有 _analyzeGoals 不回归 | ✅ 已完成 |
| T17-2 | 目标级推进洞察（`_analyzeGoals` 扩展）：①「推进最快目标」——周期内 DA 完成率最高且 ≥1 的目标（naming 具体目标名）；②「停滞目标」——active 目标周期内 DA=0 且 KR 未全部达成（给出「建议：减少日行动密度或暂停」action）；③「目标完成里程碑」——KR 已全部达成（currentValue≥targetValue）的目标；④ 跨模块：目标推进率 vs 日程完成率联动（目标类事项的完成拖累目标推进） | `src/lib/brains/reviewer.ts` | 无（只读） | 有目标数据时产出 ≥1 条目标级洞察（带具体目标名），无数据时静默跳过 | ✅ 已完成 |
| T17-3 | 首页复盘洞察联动（`HomeReview.tsx`）：确认目标级洞察自然流入首页 Top 4 洞察流；「目标推进」洞察卡片点击跳转 `/efficiency-v2`（已有模块映射，核对生效） | `src/components/dashboard/HomeReview.tsx` | 无 | 首页洞察流出现目标级卡片，点击可达目标页 | ✅ 已完成（映射已生效，无需改动） |
| T17-4 | 长期主义目标模块展示增强（`/longtermism` 复盘区 goals 模块）：目标级数据（推进最快/停滞目标名）注入模块洞察卡片。**走查发现并修复截断 bug**：次发现渲染 `finding.description.replace(/^.{0,4}/, "")` 会删掉描述前 4 字符（目标名「走查测试目标」被截成「试目标」、日期「2026-08-06」被截成「-08-06」）→ 移除该 hack 直接渲染完整 description | `src/app/longtermism/page.tsx` | 无 | 长期主义复盘目标卡片展示目标级信息且文案完整 | ✅ 已完成 |
| T17-5 | 验证（typecheck + 浏览器走查：有目标数据的复盘输出目标级洞察、无目标时静默）+ GUIDE/PLAN 登记 + git 提交推送 | 全量 | — | 无回归，文档同步 | ✅ 已完成 |

> 📌 **T13 验收标准勘误**：原定「数据库表数下降 30%+」口径有误——全库实为 10 个 `LifeFlow*` 库共 98 张表（含遗留 LifeFlowDB 主库 48 表等，非本任务范围），全库口径下删 7 张仅 -7.1%。实际成效以**涉及的两库**为准：效率库 6→4（-33.3%）、健康库 17→12（-29.4%）。deprecated 表清零即达标。

### 4.3 优先级矩阵（Top 项）

| 任务 | 用户价值 | 成本 | 依赖 | 分 | 阶段 |
|------|---------|------|------|----|------|
| T1 目标tab切换 | 高 | 低 | 低 | 高 | P0 |
| T2 /more总入口 | 高 | 低 | 低 | 高 | P0 |
| T3 隐藏plugins | 中 | 低 | 低 | 中高 | P0 |
| T6 饮水口径统一 | 高 | 中 | 中 | 中高 | P1 |
| T9 v1→v2迁移 | 高 | 高 | 高 | 中 | P2 |
| T13 表清理 | 中 | 高 | 高 | 低中 | P3 |

---

## 五、附录：与 LIFEFLOW_GUIDE.md 的冲突项清单

| # | GUIDE 现有约定 | 本方案变更 | 冲突性质 | 建议处理 |
|---|---------------|-----------|---------|---------|
| 1 | 底部导航为 4-tab（首页/目标/日程/长期主义） | 保持 4-tab 数量，但目标 tab 指向 `/efficiency-v2` | 部分打破（路径变更，数量不变） | 更新 GUIDE 目标 tab 路径说明 |
| 2 | 「目标-阶段-任务-事项 层级 [已废弃]」仅保留代码 | P2 T9 提供 v1→v2 数据迁移 | 不冲突，补充迁移方案 | GUIDE 追加迁移工具说明 |
| 3 | 饮水数据展示统一口径（多处打补丁） | P1 T6 明确 waterLogs 为唯一流水源 | 不冲突，收敛口径 | GUIDE 追加数据源裁决 |
| 4 | plugins 体系（plugin-config.ts） | P0 T3 隐藏入口、P2 T12 下线 | 打破 | GUIDE 追加 plugins 退役记录 |
| 5 | 艾宾浩斯入口「仅长期主义卡片」 | 保持不变 | 无 | — |

---

## 附：待确认清单（执行前需用户决策）

1. 底导目标 tab 切换是否为最高优先？（T1）
2. `/more` 总入口的模块分类是否认可（习惯组/生活组/学习组/管理组）？
3. plugins 是否同意隐藏入口（路由保留）？
4. v1→v2 数据迁移是否需要在本次路线图内实施（还是只列计划）？

---

## 六、T18 能量金字塔重构（v2.8+，已确认执行）

> **背景**：T17 后用户提出全链路重构需求。经 5 阶段提示词工程工作流（GUIDE 上下文初始化 → 需求挖掘 → 漏洞检查 → 正式提示词 → 确认门禁）确认最终决策：
> - **分层**：P-1 能量金字塔（E1 能量底座 → E2 目标执行 → E3 过程记录 → E4 成长储备 + ⚙ 维修模式）
> - **底导**：T-A 维持 4-tab（今日/目标/日程/长期主义）
> - **冗余**：D-A 死表物理清除
> - **视觉**：保持 iOS 卡片风；路由允许科学合理调整

### 6.1 正式任务提示词（终版 v3，用户确认）

<details>
<summary>📋 正式任务提示词（点击展开）</summary>

```markdown
# 正式任务提示词：LifeFlow 能量金字塔重构（T18）

## 【角色设定】
你是 LifeFlow（个人生活操作系统）的首席产品设计师兼前端工程师。
使命：把 19+1 个功能按「能量金字塔」因果依赖链分层重构，
让用户从打开 App 到完成一次有效动作的路径最短、干扰最少。
严格遵循第一性原理：分层表达「因果依赖链」——睡眠是一切的地基，
能量充足才能执行目标；条件性功能（如吃药）平时隐藏、需要时激活，
绝不常驻打扰。

## 【任务背景】
LifeFlow 当前 19+1 功能平铺于 4-tab 下，长期主义页堆叠 13 卡、more 页 19 入口。
用户核心诉求：「睡眠是最重要的，保证睡眠才能开展其他」「吃药提醒不是天天
都要做，只有生病才需要」。已确认决策：
- 分层：P-1 能量金字塔（E1 能量底座 / E2 目标执行 / E3 过程记录 / E4 成长储备 + ⚙ 维修模式）
- 底导：T-A 维持 4-tab（今日/目标/日程/长期主义）
- 冗余：D-A 死表物理清除
- 视觉：保持 iOS 卡片风；路由允许科学合理调整

## 【执行步骤】

### 第 1 步：数据库死表物理清除（D-A）
1.1 编写浏览器脚本，对主库 LifeFlowDB 全部表 count() 审计，标记 0 行且无 UI 消费的表
1.2 新增主库 v34：将确认无消费的表置 null 物理删除（goals/plans/tasks/habit_logs/
     goalTemplates/customGoalTypes/correlationReports/boards/sections/projectV2s 等，
     仅删除确认死代码的表；reminders/userSettings/finRecords/events 等活跃表保留）
1.3 删除死文件：aiGoalGenerator.ts / aiReviewAnalyzer.ts / correlationAnalysis.ts
     （确认无 import 消费后）
1.4 验证：四 tab 功能正常，reminders 提醒不失效

### 第 2 步：能量金字塔分层落地（P-1）
2.1 功能归属表：
     - E1 能量底座：睡眠、作息、饮水、饮食
     - E2 目标执行：目标五层、日程时间轴、专注、习惯
     - E3 过程记录：记账、训练、养生、体态拉伸
     - E4 成长储备：复盘、记忆复习、心愿、备忘录、课程表
     - ⚙ 维修模式（条件激活）：吃药 —— 有用药计划/生病记录时才浮现，平时隐藏
2.2 路由：保持 /efficiency-v2、/efficiency/schedule 不变；/more/* 按层归组不改路由名

### 第 3 步：首页重构为「今日驾驶舱」
3.1 顶部 E1 能量区：睡眠（昨晚时长+质量）、饮水（今日杯数）、作息状态
     三合一卡片组，最大视觉权重，点击跳对应模块
3.2 中部 E2 今日执行：目标日行动 + 日程待办合并为一条时间流
3.3 底部复盘洞察：保留 HomeReview Top 4
3.4 E3/E4 入口收敛为小图标行；吃药维修模式不在首页出现

### 第 4 步：长期主义页重构
4.1 复盘区保留（UnifiedReviewer）
4.2 模块卡片按金字塔分组：
     E1 能量底座（睡眠/作息/饮水/饮食）置顶突出
     E2（习惯/专注）居中
     E3/E4 折叠区（记账/训练/养生/体态/复盘/记忆/心愿/备忘录/课程表）
4.3 卡片保持数据展示（核心数据 + 行动引导）

### 第 5 步：目标页 / 日程页布局优化
5.1 目标页：突出「活跃目标 + 今日日行动」，弱化创建向导视觉
5.2 日程页：时间轴保持，顶部加「今日焦点」（高优先级事项）

### 第 6 步：维修模式机制（吃药条件激活）
6.1 吃药模块从常驻入口移除，改为条件触发：
     - 有用药计划（healthDB.medications 有记录且启用）时浮现入口
     - 或用户手动在设置中开启「吃药提醒」后显示
6.2 首页/长期主义/更多页均按此规则渲染，平时完全不可见

### 第 7 步：页面布局与入口设计规范
7.1 布局骨架：全站保持 `max-w-[430px] mx-auto` 移动优先容器；
     底部 4-tab 固定导航（今日/目标/日程/长期主义）；
     页面头部统一「标题 + 返回 + 右侧操作」三段式
7.2 入口层级（按金字塔视觉权重递减）：
     - E1 入口：卡片式大入口（h-24+，展示实时数据）
     - E2 入口：底部 tab 直接承载
     - E3 入口：列表行 + 缩略数据
     - E4 入口：折叠区/小图标
     - 维修模式：无入口（条件激活后临时出现）
7.3 更多页（/more）按 E3/E4 分组重排：日常记录单列展开、
     按需工具与长期资产默认折叠
7.4 入口语义：每个入口必须同时具备 lucide 图标 + 中文标签 +
     实时数据摘要（如「睡眠 7h32m」而非仅有「睡眠」）

### 第 8 步：设计元素规范（图标/头像/组件/字体/CTA/图片）
8.1 图标：统一 lucide-react，strokeWidth=2，尺寸规范
     （tab=24、入口图标=22、列表行=18、折叠箭头=16）；
     颜色沿用各模块既有主题色（sleep=#1E293B、water=#0EA5E9 等）
8.2 头像：本项目为个人工具不设用户头像；如需占位用
     首字母圆形（w-10 h-10 rounded-full bg-primary/10 文字首字）
8.3 组件样式：统一复用既有组件（Card=rounded-2xl bg-white
     dark:bg-gray-900 border border-gray-100 dark:border-gray-800
     shadow-sm、Button=rounded-xl、Sheet=rounded-t-2xl、Input=rounded-lg），
     禁止新建风格不一致的自定义组件
8.4 字体：保持 iOS 字体栈（--font-sans/-apple-system/SF Pro Text/PingFang SC），
     字号层级沿用 globals.css（title=34/700、nav=17/600、section=20/700、
     body=17/400、caption=13/500）；禁止引入 web font
8.5 CTA：主 CTA 用模块主题色填充 + white 文字（rounded-xl py-3）；
     次 CTA 用 outline；破坏性操作用 #FF3B30；CTA 文案统一「动词+对象」
     （如「记录睡眠」而非「保存」）
8.6 图片素材：本应用为数据型个人工具，不引入产品图片/插画；
     空状态用 lucide 图标（48px，--lifeflow-muted）+ 引导文案；
     禁止使用网络占位图
8.7 色彩：沿用 globals.css CSS 变量体系（--lifeflow-*），
     新增层级色阶时追加变量，禁止硬编码十六进制色值散落页面
8.8 动效：卡片/Sheet 过渡沿用 framer-motion 既有实例，
     禁止新增全局动画库；展开动画遵循 GUIDE「禁止 overflow-hidden」约定

### 第 9 步：视觉层级统一
9.1 保持 CSS 变量 + iOS 卡片风，不引入新主题
9.2 用尺寸/色阶/间距表达层级：E1 最大最醒目，E4 最小最收敛
9.3 全站中文，遵循 LIFEFLOW_GUIDE.md 全部既有约定
9.4 数据不迁移不删除，19+1 功能全部保留

## 【输出规范】
- 逐层给出功能归属表（已见 2.1）
- 每个改动页面给出「改前 → 改后」布局说明（含入口层级变化）
- 遵循 Next.js App Router 结构
- 全部中文注释
- 完成后 `npx tsc --noEmit` 零类型错误
- 提交前浏览器走查：4-tab 全部可访问、E1 健康区数据正确、
  吃药维修模式隐藏/激活正确、more 折叠正常、图标/字号/CTA 视觉一致

## 【边界与例外】
- 数据迁移需求 → 停止并评估，不擅动数据
- 死表清除前必须 count() 审计确认死代码，否则跳过
- 吃药维修模式若无现成「用药计划」数据模型 → 用 settings 开关兜底实现
- 视觉分歧 → 以「效率优先 + iOS 卡片风」为准
- 涉及新增图片素材/头像/新字体 → 一律拒绝（按 8.2/8.4/8.6 规范）
- 单步超范围 → 拆分并与用户确认
```
</details>

### 6.2 T18 执行检查清单（用户确认，验收标准以此为准）

| # | 步骤 | 关键检查项 | 验收标准 |
|---|------|-----------|---------|
| 1 | D-A 死表清除 | 1.1 全表 count() 审计；1.2 主库 v34 死表置 null；1.3 删 3 死文件（grep 0 import）；1.4 活跃表保留 | tsc 零错误；刷新无 SchemaError；四 tab 正常；reminders 不失效 |
| 2 | P-1 分层落地 | 2.1 归属表无遗漏（19+1 全覆盖）；2.2 路由不变 | 各层入口可达 |
| 3 | 首页驾驶舱 | 3.1 E1 三合一（睡眠/饮水/作息实时数据）；3.2 今日执行合并流；3.3 HomeReview；3.4 无吃药入口 | 页面结构=能量区→执行→洞察，数据真实 |
| 4 | 长期主义分组 | 4.1 复盘区保留；4.2 E1 置顶→E2→E3/E4 折叠；4.3 卡片数据+行动引导 | 分组顺序与金字塔一致，折叠可展开 |
| 5 | 目标/日程优化 | 5.1 活跃目标+今日日行动为焦点；5.2 时间轴+今日焦点 | 无功能回归（创建/勾选/删除可用） |
| 6 | 维修模式 | 6.1 无条件时全站无吃药入口；6.2 有条件时浮现；6.3 settings 兜底 | 两种状态可切换且刷新保持 |
| 7 | 布局/入口规范 | 7.1 430px 容器+4-tab+三段式头；7.2 入口层级视觉递减；7.3 more 分组；7.4 图标+标签+数据摘要 | 抽查 5 入口齐全且层级可区分 |
| 8 | 设计元素规范 | 8.1 lucide 统一；8.2 无头像用首字母圆；8.3 复用既有组件；8.4 iOS 字体无 web font；8.5 CTA 规范；8.6 无图片素材；8.7 用变量无色值散落；8.8 动效沿用 | grep 无新增硬编码色值/无新字体/无 img |
| 9 | 视觉统一+收尾 | 9.1 无新主题；9.2 E1→E4 层级表达；9.3 数据保留（各库 count 前后一致）；9.4 GUIDE/PLAN 登记 | tsc 零错误 + 走查全通过 |

### 6.3 最终验收（T18 整体）

- [x] `npx tsc --noEmit` 零错误（2026-08-06 验证通过）
- [x] 4-tab 全部可访问，无 404（浏览器走查通过）
- [x] E1 健康区数据正确（睡眠/饮水真实数据，浏览器走查通过）
- [x] 吃药维修模式隐藏/激活正确（settings 开关 + useMedicineMode 条件渲染）
- [x] more 折叠正常（MODULE_GROUPS 按 E1-E4+维修+系统分组，折叠区可展开）
- [x] 图标/字号/CTA 视觉一致（grep 无新字体/无 img/无硬编码色值散落，lucide 统一）
- [x] 数据未丢失（重构前后各库 count 对比一致：死表 0 行、活表保留）
- [x] git commit + push（commit fb6d3b6）

### 6.4 T18 任务表（执行中）

| # | 任务 | 改动范围 | 影响数据 | 验收标准 | 状态 |
|---|------|---------|---------|---------|------|
| T18-1 | 死表物理清除（D-A）：主库 v34 置 null `habit_logs`/`goalTemplates`/`customGoalTypes`/`correlationReports`（goals/plans/tasks 经审计有活跃消费保留）；删 4 死文件 aiGoalGenerator/aiReviewAnalyzer/correlationAnalysis/customGoalTypes | db.ts v34 + 删 4 文件 + linkage.ts 清理 | 无（死表 0 行，活表保留） | tsc exit 0；无 SchemaError；四 tab 正常；reminders 不失效 | ✅ 已完成 |
| T18-2 | P-1 金字塔分层落地：E1-E4+维修模式归属（见 2.1），路由保持不变 | 各页面入口 | 无 | 19+1 全覆盖无遗漏 | ✅ 已完成（more 页按 E1-E4+维修+系统分组） |
| T18-3 | 首页重构「今日驾驶舱」：E1 三合一健康卡（睡眠/饮水/作息）+ E2 今日执行合并流 + HomeReview + E3/E4 小图标行 | src/app/page.tsx | 无 | 结构=能量区→执行→洞察 | ✅ 已完成 |
| T18-4 | 长期主义页金字塔分组：E1 置顶突出 / E2 居中 / E3/E4 折叠区 | src/app/longtermism/page.tsx | 无 | 分组顺序与金字塔一致 | ✅ 已完成 |
| T18-5 | 目标页/日程页优化：目标页突出活跃目标+今日日行动；日程页顶部今日焦点 | efficiency-v2 列表页 + schedule 页 | 无 | 无功能回归 | ✅ 已完成 |
| T18-6 | 吃药维修模式：条件激活（用药计划或 settings 开关），平时全站隐藏 | health.db + 首页/长期主义/more + settings | 无 | 两状态可切换刷新保持 | ✅ 已完成（useMedicineMode + settings 开关） |
| T18-7 | 布局与入口规范：430px 容器 + 三段式头 + 入口层级 + more 分组 + 入口语义 | 全局页面 | 无 | 抽查 5 入口达标 | ✅ 已完成（15 处 430px 容器、more 按 E1-E4+维修+系统分组） |
| T18-8 | 设计元素规范：lucide 统一/组件复用/iOS 字体/CTA/无图片/色彩变量/动效 | 全局组件 | 无 | grep 无违规 | ✅ 已完成（grep 无 web font/无 img/无硬编码色值散落） |
| T18-9 | 验证（typecheck + build + 浏览器走查）+ GUIDE/PLAN 登记 + git 提交推送 | 全量 | — | 无回归，文档同步 | ✅ 已完成（tsc 0 错误 + build 41 路由 + 走查 PASS；走查修复：E34 折叠区补齐 复盘/备忘录/课程表并对齐清单 4.2、折叠区真正折叠） |

## 七、T19 理想日系统（v2.9+，已确认执行）

### 7.1 正式任务提示词（融合版，用户确认）

# 正式任务提示词：LifeFlow「理想日系统」科学升级

## 【角色设定】
你是精通产品架构、行为科学（执行意图/时间预算/习惯形成）与第一性原理的个人效率产品专家。熟悉 LifeFlow 现有架构与 LIFEFLOW_GUIDE.md 全部约定，擅长把"理想的一天"翻译为可定义、可排程、可执行、可复盘的每日闭环。

## 【任务背景】
用户实际作息与理想严重偏离（1点睡/8点醒、饮水500ml、刷视频失控）。理想日：6:00 醒、7:00 前洗漱早餐、7:00-8:00 健身、8:30 起学习（2027 省考为主，学习时间 ≥ 四级 2 倍）、22:30 入睡 + 12:30 午睡共 8h；8h 学习工作（省考:四级 ≥ 2:1 科学分配）+ 8h 留给自己（运动/娱乐/社交）；饮水 2000ml 三时段；健身沿用现有规划（周一/三/五力量 + 周二/四有氧 + 3大项每月一主两副）；拖延治理采用「执行意图 + 数据反馈 + 娱乐配额」组合。LifeFlow 需升级为承载这套"理想日"的行为闭环工具。

## 【执行步骤】
1. 实现「理想日蓝图」配置页：8+8+8 结构、睡眠窗口、学习分配比例、娱乐配额、饮水目标、健身频率（复用训练生成器配置）。
2. 蓝图 → 自动排程：生成作息模板 + GoalV2 双目标日行动（省考≥2×四级）+ 训练计划 + 饮水三时段 + 自由时间留白块。
3. 实现执行引导：时间块前提醒、自由时段执行意图弹窗、学习时段免打扰、娱乐配额超时飘红。
4. 实现「理想日达成率」：睡眠+学习+饮水+配额四维达成，接入 UnifiedReviewer 复盘，首页/长期主义展示。
5. 登记到 LIFEFLOW_OPTIMIZATION_PLAN.md（T19 章节）后实施。

## 【输出规范】
- 中文方案 + 代码实现；遵循能量金字塔分层、4-tab 导航、单一入口、数据不丢、Dexie 升级保留旧版本定义等 GUIDE 约束。
- 学习排程必须给出省考:四级 ≥ 2:1 的具体时间槽（如 8:30-12:30 省考 + 14:00-16:00 四级 + 16:00-18:00 省考，示例可调）。
- 养生功法仅以"功法养生"呈现，不出现敏感表述。

## 【边界与例外】
- 不删除任何历史数据；不推翻现有架构；不改现有表结构（仅加字段/新表）。
- 拖延治理不做设备级强制封锁（网页内不可达，列为后续）。
- 涉及 DB 版本升级必须先登记 PLAN 并经用户确认。
- 任一未确认项（如娱乐配额数值、学习时段拆分）导致无法继续时，暂停并请求用户补充。

### 7.2 需求基线（阶段1-2 挖掘结论）

- **睡眠**：8h = 夜间 22:30→6:00（7.5h）+ 午睡 12:30（0.5h）
- **学习**：8h/天，省考:四级 ≥ 2:1 科学分配；2027 省考（重点）+ 2026.12 四级
- **健身**：周一/三/五 健身房复合力量（1h）+ 周二/四 低强度有氧 + 3 大项每月一主两副（主项跟力量日、副项周六）——与训练计划生成器 `weeklyDays: [1,3,5]`/`[2,4]` 对齐
- **饮水**：2000ml，上午/下午/晚 8:00 前三时段（沿用 T15 三时段目标制）
- **自由时间**：学习/工作/睡觉之外全部（运动/娱乐/社交）
- **拖延治理**：执行意图（主）+ 娱乐配额（可选）+ 数据反馈（周复盘对比）组合；设备级封锁列为后续演进

### 7.3 T19 执行检查清单（验收标准以此为准）

| # | 检查项 | 验收标准 |
|---|--------|---------|
| C1 | 理想日蓝图配置页 | 可配置 8+8+8 结构、睡眠窗口、学习分配比例、娱乐配额、饮水目标、健身频率 |
| C2 | 蓝图→自动排程 | 配置一次即生成完整一天：作息 + 双目标学习（省考≥2×四级）+ 训练 + 饮水 + 留白块 |
| C3 | 执行引导 | 时间块前提醒、自由时段执行意图弹窗、娱乐配额超时飘红 |
| C4 | 理想日达成率 | 睡眠+学习+饮水+配额四维达成率接入 UnifiedReviewer，首页/长期主义展示 |
| C5 | 数据安全 | 不删历史数据、不改现有表结构（仅加字段/新表）、Dexie 升级保留旧版本定义 |
| C6 | 无回归 | typecheck 0 错误；4-tab 正常；现有功能不受影响 |
| C7 | 文档同步 | GUIDE 更新 T19 约定；PLAN 任务表登记；git commit + push |

### 7.4 T19 任务表（执行中）

| # | 任务 | 改动范围 | 影响数据 | 验收标准 | 状态 |
|---|------|---------|---------|---------|------|
| T19-1 | 理想日蓝图配置（数据模型 + 配置页） | userSettings 新字段 idealDayConfig + /more/ideal-day 配置页 | 无 | 配置页可读写全部蓝图项 | ✅ 已完成（typecheck 通过） |
| T19-2 | 蓝图→自动排程（作息模板 + 双目标 DA + 训练 + 饮水 + 留白） | ideal-day.ts 排程引擎 + 配置页联动 + 日程页联动 | 新增 items（不删旧） | 生成一天=作息+学习+训练+饮水+留白 | ✅ 已完成（学习块按省考≥2×四级绕开午睡分段；作息模板同步；训练日判定复用训练计划 weeklyDays；留白块睡前配额窗口；临时脚本验证 5 场景全过 + tsc 0 错误） |
| T19-3 | 执行引导（块前提醒 + 执行意图弹窗 + 娱乐配额飘红） | 日程页 + 首页 + 提醒调度 | 无 | 三机制可触发可关闭 | ✅ 已完成（ideal-day-guide.ts hook + 日程页/首页横幅 + 执行意图弹窗 + 配额超时卡片飘红；localStorage 存执行意图；tsc 0 错误） |
| T19-4 | 理想日达成率接入复盘 | reviewer.ts + 首页/长期主义 | 无 | 四维达成率展示 | ✅ 已完成（reviewer.ts 新增 ideal 模块：睡眠=夜间按时+午睡完成 / 学习=ideal-study 块完成分钟 / 饮水=waterLogs≥蓝图目标 / 配额=leisure 块完成率，四维平均整体达成率+薄弱维度发现，MODULE_COLORS/ICONS 注册，首页 MODULE_ROUTES 指向 /more/ideal-day；tsc 0 错误） |
| T19-5 | 验证 + GUIDE/PLAN 登记 + git 提交推送 | 全量 | — | tsc 0 错误 + 走查 PASS + 文档同步 | ✅ 已完成（tsc 0 错误；GUIDE 新增「理想日系统」约定；PLAN 任务表 T19-1~5 全部登记；git commit 1697b4b + push main。端到端走查 5/5 PASS：开关/保存/7 天排程幂等、学习块绕午睡分段、周五训练日命中、自由时间 90min 窗口、首页+长期主义理想日洞察上线；验证后已恢复关闭） |
