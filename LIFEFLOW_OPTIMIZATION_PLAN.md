# LIFEFLOW 产品优化整合方案

> 生成日期：2026-07-30
> 性质：全面体检审计 + 信息架构重设计 + 优化路线图（整合文档）
> 基线：基于当前代码库实际状态（路由 / 数据库 / 导航 / 组件）与 `LIFEFLOW_GUIDE.md` 现有约定

---

## 〇、产品定位与目标用户

### 定位结论

LIFEFLOW 不是「待办清单」，而是 **目标驱动的个人管理系统（Life OS）**：

- 核心引擎是「目标五层拆解」（愿景 → 关键结果 → 策略 → 周任务 → 日行动），一切围绕目标展开
- 日程时间轴承担「执行层」（作息/课程/事项/饮水/专注/记账全落进时间轴）
- 长期主义承担「习惯与长期维度」（健康/财务/学习/心愿）
- 复盘引擎（UnifiedReviewer）承担「闭环反哺」（日/周/月/年复盘）

### 目标用户画像

| 维度 | 定义 |
|------|------|
| 主画像 | 有明确长期目标的自我提升者：备考学生（四六级/国考/考研）、职场新人（健身/理财/技能成长） |
| 核心需求 | 不是"记录生活"，而是"实现目标"——把人生目标拆到每天可执行，持续跟踪与复盘 |
| 价值主张 | 一站式「目标 → 计划 → 执行 → 复盘」个人管理闭环 |
| 使用节奏 | 早晨看首页今日执行 → 目标页拆解规划 → 日程页执行打卡 → 长期主义记录习惯 → 复盘洞察反哺 |

### 主线用户路径（作为信息架构设计的决策依据）

```
定目标(五层拆解) → 排计划(策略/周任务/日行动) → 每日执行(日程时间轴) → 记录习惯(长期主义) → 复盘洞察(首页/长期主义)
```

---

## 一、功能模块清单（覆盖全部网站）

### 1.1 核心 4-tab

| Tab | 路由 | 实际内容 | 状态 |
|-----|------|---------|------|
| 首页 | `/` | 复盘洞察 + 饮水提醒 + 今日待办 + 倒数日 + FAB | ✅ 正常 |
| 目标 | `/efficiency` | **旧版目标系统 v1**（`LifeFlowEfficiency` 库） | ⚠️ 已废弃，应切换 |
| 日程 | `/efficiency/schedule` | 时间轴（折叠显示/冲突检测/作息课程自动生成） | ✅ 正常 |
| 长期主义 | `/longtermism` | 复盘时间轴 + 9 张模块卡片 + 历史复盘 | ✅ 正常 |

### 1.2 新版目标系统（独立于 4-tab）

| 路由 | 内容 | 状态 |
|------|------|------|
| `/efficiency-v2` | 目标列表（五层拆解，AI 导入） | ✅ 新版核心 |
| `/efficiency-v2/new` | 5 步创建向导（模板 + AI 提示词导入） | ✅ 正常 |
| `/efficiency-v2/goals/[id]` | 目标详情 | ✅ 正常 |

### 1.3 长期主义模块卡片（9 张）

饮水 `/more/water`、睡眠 `/more/sleep`、记账 `/more/accounting`、训练 `/more/fitness`、饮食 `/more/diet`、养生 `/more/wellness`、体态拉伸 `/more/posture`、心愿 `/more/wishes`、记忆 `/more/ebbinghaus`

### 1.4 /more 子模块（非卡片入口，入口在 /more/projects）

| 路由 | 功能 | 备注 |
|------|------|------|
| `/more/projects` | 全部功能聚合导航 | 首页「功能模块」按钮直达 |
| `/more/calendar` | 日历月视图 | |
| `/more/countdown` | 倒数日 | 首页有卡片 |
| `/more/focus` | 专注计时（番茄钟） | 新 |
| `/more/habits` | 习惯打卡 | 新 |
| `/more/medication` | 吃药提醒 | |
| `/more/notes` | 备忘录 | |
| `/more/reminder-settings` | 默认提醒设置 | |
| `/more/review` + 6 子页 | 各模块复盘（部分为占位文案） | ⚠️ 部分占位 |
| `/more/schedule/courses` | 课程表 | |
| `/more/schedule/routines` | 作息模板组 | |
| `/more/accounting/{ledgers,assets,categories}` | 记账子管理 | |

### 1.5 其他路由

| 路由 | 功能 | 状态 |
|------|------|------|
| `/tasks` | 任务分类（日程页"分类"入口） | 正常 |
| `/reminders` | 提醒列表 | 正常 |
| `/assistant` | AI 助手（全局悬浮球进入） | 正常 |
| `/settings` | 设置（数据迁移/个性化） | 正常 |
| `/focus` | 专注（日程页快捷操作跳转） | 与 `/more/focus` 并存 |
| `/exercise` | 训练（MusclePage） | 与 `/more/fitness` 并存 |
| `/plans/[planId]` | 训练计划详情 | 归属模糊 |
| `/trash`、`/pending`、`/offline`、`/widget` | 辅助页 | 边缘功能 |
| `/plugins/*`（5 个） | 旧版插件中心 | ⚠️ 全部有新版替代 |

### 1.6 数据库全景（7 个 Dexie 实例）

| 数据库 | 表数 | 最高版本 | 状态 |
|--------|------|---------|------|
| LifeFlowHealth | 12 | v12 | ✅ 正常（T13 后旧表已清零） |
| LifeFlowDaylog | 4 | v6 | ✅ 核心（items 为主表） |
| LifeFlowGoalV2 | 5 | v2 | ✅ 新版目标核心 |
| LifeFlowEfficiency | 4 | v19 | ✅ 正常（T13 后 tasks/habits 已删，goals/scheduleTasks/projects/phases 活跃） |
| LifeFlowEbbinghaus | 3 | v1 | ✅ 新 |
| LifeFlowAccounting | 4 | v4 | ✅ 正常 |
| LifeFlowLife | 8 | v7 | ✅ 正常 |

---

## 二、现状审计报告

### 2.1 问题分级清单

| 级别 | # | 问题 | 影响 | 证据 |
|------|---|------|------|------|
| 🔴 高 | 1 | **底导「目标」tab 仍指向旧版 `/efficiency`，而新版目标在 `/efficiency-v2`** | 用户从导航进入的是废弃系统，功能双轨、数据分裂 | BottomTabBar 目标 tab = `/efficiency`；首页空态"目标"按钮 = `/efficiency-v2` |
| 🔴 高 | 2 | **旧版 `/plugins` 体系与新功能重复（5 个插件均有新版替代）** | 入口混乱、维护双份、样式割裂（旧灰白 vs 新版令牌） | plugins/habit→more/habits、plugins/finance→more/accounting 等 |
| 🔴 高 | 3 | **`LifeFlowEfficiency` 库（v15）仍在线上** | 冗余存储、迁移负担；⚠️ 审计时"6 张表无数据产出"的结论已过时——AI 助手/作息同步/任务页仍活跃写入 goals/scheduleTasks/projects（T8 已软冻结 habits/tasks 死表，完整冻结依赖 T9 迁移后） | GUIDE 明确标注"已废弃，不迁移，仅保留代码" |
| 🟠 中 | 4 | **health.db 17 张表新旧重复**：`waterRecords`/`waterLogs`、`sleepRecords`/`sleepLogs`、`fitnessRecords`+`exercises`+`muscleGroups` vs `muscleGroupsV2`+`exercisesV2`+`workoutSessions` | 数据口径分裂、代码查询易错 | 同域数据多表并存 |
| 🟠 中 | 5 | **饮水数据双轨**：`daylog.items(sourceType=water)` 与 `health.waterLogs` 并存 | 统计口径不统一（GUIDE 已多次为口径打补丁） | 首页/长期主义/饮水页三种查询 |
| 🟠 中 | 6 | **`/more` 无总入口页**，靠 `/more/projects` 动态聚合，功能可达性差 | 新功能入口深，用户找不到 | /more 目录无 page.tsx |
| 🟠 中 | 7 | **`/more/review/*` 部分子页为占位文案** | 无效页面，用户点击无收益 | review/{finance,fitness,...} 部分为"引导用 AI 助手" |
| 🟢 低 | 8 | `/focus` 与 `/more/focus`、`/exercise` 与 `/more/fitness`、`/plans/[planId]` 归属模糊 | 双入口、职责不清 | 同功能两套路由 |
| 🟢 低 | 9 | 长期主义卡片 9 张未覆盖专注/习惯/倒数日/备忘录/吃药/日历/课程/作息 | 核心习惯入口依赖 /more/projects | MODULES 数组仅 9 项 |
| 🟢 低 | 10 | `Common/TabBar.tsx`、`ui/woven/` 等组件疑似无人使用 | 死代码膨胀 | 子代理核验 |

### 2.2 优势盘点（保留）

- 目标五层拆解 + AI 导入闭环成熟（模板/提示词/解析器/冲突检测）
- 日程时间轴体验完整（作息课程自动生成/折叠/冲突/校准/复盘）
- 复盘引擎（UnifiedReviewer）已统一，首页与长期主义共用
- 数据层分离清晰（业务库拆分），版本升级流程规范（GUIDE 约束）
- 全局组件体系完善（BottomSheet/ActionSheet/Toast/Skeleton/ErrorBoundary）

---

## 三、新信息架构方案

### 3.1 架构原则

1. **目标主线**：一切入口围绕「目标 → 执行 → 长期」三层展开
2. **单一入口**：同一功能只保留一个入口、一套数据、一条路由
3. **旧版退役**：旧版功能（plugins/efficiency v1）逐步隐藏入口，路由保留但不可达
4. **渐进迁移**：不删除功能、不丢数据，先切入口后迁数据

### 3.2 新导航结构（建议）

```
底部 4-tab（保持 4 个，重定义第 2 个）
├── 首页        `/`                       （不变）
├── 目标        `/efficiency-v2`          （从 /efficiency 切换 ✅ 高优先）
├── 日程        `/efficiency/schedule`    （不变）
└── 长期主义    `/longtermism`            （不变）

功能目录（"全部功能"单一入口）
└── `/more` 总入口页（新增 page.tsx，替代 /more/projects 的隐式入口）
    ├── 习惯组：饮水/睡眠/训练/饮食/养生/体态/习惯/吃药/专注
    ├── 生活组：记账/心愿/倒数日/备忘录/日历
    ├── 学习组：记忆（艾宾浩斯）
    └── 管理组：课程表/作息/提醒设置/复盘
```

### 3.3 逐项变更与科学依据

| # | 变更 | 依据 | 风险 | 是否打破现有约定 |
|---|------|------|------|----------------|
| C1 | 底导「目标」tab 路由改为 `/efficiency-v2` | 新版是唯一活跃目标系统；首页/复盘/创建向导均指向 v2 | 低（改一行路由） | 打破「目标 tab=/efficiency」隐含约定 |
| C2 | 新增 `/more` 总入口页（静态分类目录） | /more 无 page.tsx，功能可达性差 | 低 | 否 |
| C3 | `/more/projects` 保留但降级为内部页（或合并进 /more） | 避免双"全部功能"入口 | 中 | 否 |
| C4 | 隐藏 `/plugins` 入口（路由保留，底导/首页不再链接） | 5 插件均有新版替代，避免用户进入旧版 | 低 | 打破「plugins 中心」约定（GUIDE 未明文，可执行） |
| C5 | 长期主义卡片扩展（补齐专注/习惯/吃药/倒数日） | 核心习惯入口过深 | 中 | 否 |
| C6 | 明确 `/focus` 与 `/more/focus`、`/exercise` 与 `/more/fitness` 归属（保留主入口，另一跳转） | 双入口职责不清 | 低 | 否 |
| C7 | `/more/review/*` 占位子页补充内容或从目录移除 | 无效页面 | 低 | 否 |

> 关于打破 4-tab 数量：**建议保持 4-tab**（科学依据：移动端认知负担、现有 UI 原则约定；新增 tab 收益低，功能入口应由 `/more` 目录承载）。

---

## 四、优化路线图

### 4.1 优先级模型

```
优先级分 = 用户价值 × 0.5 + 实现成本（低=高分）× 0.3 + 依赖度（少依赖=高分）× 0.2
```

### 4.2 分阶段计划

> 进度标记：✅ 已完成 · 🔄 进行中 · ⬜ 待执行

#### P0 快赢（低风险高价值，1 周内）

| # | 任务 | 改动范围 | 影响数据 | 验收标准 | 状态 |
|---|------|---------|---------|---------|------|
| T1 | 底导「目标」tab → `/efficiency-v2` | BottomTabBar.tsx 一行 | 无 | 点目标 tab 进入新版目标列表 | ✅ 已完成 |
| T2 | 首页「功能模块」按钮直达 `/more` 总入口 | 新增 /more/page.tsx + 首页链接 | 无 | /more 展示全部分类目录 | ✅ 已完成 |
| T3 | 移除 `/plugins` 对外入口（底导/首页/更多链接） | 导航与聚合页 | 无 | 站点内无 plugins 链接可达 | ✅ 已完成 |
| T4 | `/more/review/*` 占位子页移除或补 1 行真实摘要 | review 目录 | 无 | 占位页不再出现在目录 | ✅ 已完成 |

#### P1 架构整合（2-4 周）

| # | 任务 | 改动范围 | 影响数据 | 验收标准 |
|---|------|---------|---------|---------|
| T5 | 长期主义卡片扩展（+专注/习惯/吃药/倒数日） | longtermism/page.tsx | 无 | 卡片补齐，均带实时数据 | ✅ 已完成 |
| T6 | 统一饮水数据口径：明确 waterLogs 为唯一流水源，daylog water items 仅做"待办"展示 | health.db/daylog.db 查询层 | 数据不迁移，仅统一读取 | 首页/长期主义/饮水页数值一致 | ✅ 已完成 |
| T7 | health.db 旧表归档（waterRecords/sleepRecords/fitnessRecords/exercises/muscleGroups 标记 deprecated，只读） | health.db.ts | 旧数据保留只读 | 新代码不再写旧表 | ✅ 已完成 |
| T8 | efficiency.db v15 冻结（软冻结：habits 全只读 + tasks 禁增改，保留 delete 清理） | efficiency.db.ts | 旧数据保留 | 死表不再写入；goals/scheduleTasks/projects/phases 保留（AI 助手/作息同步仍依赖） | ✅ 已完成（软冻结） |

#### P2 功能增强（1-2 月）

| # | 任务 | 改动范围 | 影响数据 | 验收标准 |
|---|------|---------|---------|---------|
| T9 | efficiency v1 目标数据**一键迁移**到 GoalV2 | 迁移工具 + 设置页入口 | 迁移后旧数据保留备份 | 迁移完成率 100%，进度/周任务/日行动对齐 | ✅ 已完成 |
| T10 | 新用户引导（首次进入高亮主线路径） | 首页/目标页 | 无 | 新用户 3 步内理解主线 | ✅ 已完成 |
| T11 | `/more` 目录支持模块搜索 | /more/page.tsx | 无 | 输入关键词可定位模块 | ✅ 已完成 |
| T12 | 旧版 plugins 页面正式下线（404 重定向到新版） | 路由层 | 无 | plugins 路由全部重定向 | ✅ 已完成 |

#### P3 远期（3 月+）

| # | 任务 | 改动范围 | 影响数据 | 验收标准 |
|---|------|---------|---------|---------|
| T13 | 数据全量归一：物理删除 deprecated 死表（T7/T8 冻结表，删除前逐张核实记录数均为 0，无数据风险） | efficiency.db v19 + health.db v12（`表名: null` 显式删表） | 无（7 张表记录数核实为 0，未触发导出备份） | ✅ deprecated 表全部物理删除：效率库 6→4 表（-33.3%）、健康库 17→12 表（-29.4%），应用运行无报错 | ✅ 已完成 |
| T14 | 目标系统深度集成：GoalV2 与日程/长期主义数据打通 | goal-v2-engine | 增量 | 目标日行动 → 日程 item → 复盘链闭环 | ✅ 已完成 |
| T15 | 系统科学化重构（三件套）：a) 课堂节奏 45+5 作息 + 饮水三时段目标制（删除每小时提醒）；b) 训练/体态拉伸/功法合并为完整训练体系；c) 全系统审计 + 页面/入口重规划 | daylog.db / health.db / reviewer / 各页面 | 增量（旧 water 待办定时清理） | ①45+5 自动切分、休息防久坐；②饮水按上午/下午/晚上(睡前2h截止)三时段目标；③训练体系单入口 | ✅ 已完成（a：45+5 课堂节奏 + 饮水三时段目标制；b：训练中心三 Tab 单入口 + 旧路由 redirect；c：第一性原理审计，清理 19 个死页/占位/死代码，proxy 断链规则归零，快捷专注 query 预填） |

> 📌 **T13 验收标准勘误**：原定「数据库表数下降 30%+」口径有误——全库实为 10 个 `LifeFlow*` 库共 98 张表（含遗留 LifeFlowDB 主库 48 表等，非本任务范围），全库口径下删 7 张仅 -7.1%。实际成效以**涉及的两库**为准：效率库 6→4（-33.3%）、健康库 17→12（-29.4%）。deprecated 表清零即达标。

### 4.3 优先级矩阵（Top 项）

| 任务 | 用户价值 | 成本 | 依赖 | 分 | 阶段 |
|------|---------|------|------|----|------|
| T1 目标tab切换 | 高 | 低 | 低 | 高 | P0 |
| T2 /more总入口 | 高 | 低 | 低 | 高 | P0 |
| T3 隐藏plugins | 中 | 低 | 低 | 中高 | P0 |
| T6 饮水口径统一 | 高 | 中 | 中 | 中高 | P1 |
| T9 v1→v2迁移 | 高 | 高 | 高 | 中 | P2 |
| T13 表清理 | 中 | 高 | 高 | 低中 | P3 |

---

## 五、附录：与 LIFEFLOW_GUIDE.md 的冲突项清单

| # | GUIDE 现有约定 | 本方案变更 | 冲突性质 | 建议处理 |
|---|---------------|-----------|---------|---------|
| 1 | 底部导航为 4-tab（首页/目标/日程/长期主义） | 保持 4-tab 数量，但目标 tab 指向 `/efficiency-v2` | 部分打破（路径变更，数量不变） | 更新 GUIDE 目标 tab 路径说明 |
| 2 | 「目标-阶段-任务-事项 层级 [已废弃]」仅保留代码 | P2 T9 提供 v1→v2 数据迁移 | 不冲突，补充迁移方案 | GUIDE 追加迁移工具说明 |
| 3 | 饮水数据展示统一口径（多处打补丁） | P1 T6 明确 waterLogs 为唯一流水源 | 不冲突，收敛口径 | GUIDE 追加数据源裁决 |
| 4 | plugins 体系（plugin-config.ts） | P0 T3 隐藏入口、P2 T12 下线 | 打破 | GUIDE 追加 plugins 退役记录 |
| 5 | 艾宾浩斯入口「仅长期主义卡片」 | 保持不变 | 无 | — |

---

## 附：待确认清单（执行前需用户决策）

1. 底导目标 tab 切换是否为最高优先？（T1）
2. `/more` 总入口的模块分类是否认可（习惯组/生活组/学习组/管理组）？
3. plugins 是否同意隐藏入口（路由保留）？
4. v1→v2 数据迁移是否需要在本次路线图内实施（还是只列计划）？
